# 冀德远健官网 - 网站编辑与预览指南

> 2026-08-13 升级完成：Gatsby 2 → 4、React 16 → 17、Webpack 5，现在兼容 Node 16~22。
> 本指南假设你已经装好 Node.js（未安装请先看"第 0 步"）。

---

## 第 0 步：确认电脑上有 Node.js（只需做一次）

打开"命令提示符"或"PowerShell"，输入：

```
node -v
```

如果显示类似 `v18.20.4` 或 `v20.x.x` 的版本号，说明已装好，跳到第 1 步。
如果提示"不是内部或外部命令"，请到 https://nodejs.org/zh-cn 下载 LTS 版本（左侧绿色按钮），一路"下一步"安装，装完重开命令提示符即可。

---

## 第 1 步：安装项目依赖（只需做一次）

在命令提示符中进入项目目录（把路径换成你电脑上的实际路径）：

```
cd D:\CC\1_madecare\project_SourceCode\official
npm install --legacy-peer-deps
```

第一次安装需要下载约 1~2 GB 的依赖包，**请耐心等它跑完**（一般 5~15 分钟，网络慢可能更久），
看到命令提示符重新出现、没有红色报错即为成功。

> 如果中途报错，最简单的处理：删掉 `node_modules` 文件夹重新 `npm install --legacy-peer-deps`。

---

## 第 2 步：启动网站预览（日常使用的命令）

```
npm run develop
```

等待 1~3 分钟，出现类似 `You can now view gatsby-starter-hello-world in the browser` 后，
用浏览器打开：**http://localhost:8000**

**这个模式叫"热更新"：你一边改文件一边保存，浏览器会自动刷新，立即看到效果。**

- 中文版首页：http://localhost:8000/
- 英文版：http://localhost:8000/en/
- 关闭预览：在命令提示符里按 `Ctrl + C`

---

## 第 3 步：日常编辑内容（改文字、换图片）

改之前不需要关掉预览，保存文件后浏览器会自动更新。

### 改文字（中文和英文）

| 想改什么 | 改哪个文件 | 说明 |
|---------|-----------|------|
| 页面上的中文文案 | `src\intl\zh.json` | 左边是编号，右边引号里是显示的文字 |
| 页面上的英文文案 | `src\intl\en.json` | 英文站显示的内容 |
| 新闻标题/摘要/详情 | `src\config.js` | NEWS_ABSTRACT（新闻列表）、NEWS_DETAIL（新闻详情） |

例子：`zh.json` 里 `"nav_home": "首页"`，把"首页"改成别的文字即可。

**重要**：改 zh.json / en.json 里的文字后，开发服务器**不会自动更新**，需要重启：
在命令提示符里按 `Ctrl + C` 停掉，再重新 `npm run develop`，然后浏览器 `Ctrl + F5` 强制刷新。
（改 config.js 里的新闻内容同理，也建议重启；改图片和 .js/.scss 文件则不需要重启，会自动更新。）

### 换图片

| 想改什么 | 目录 |
|---------|------|
| 首页、产品、解决方案配图 | `src\assets\imgs\` 下对应子文件夹 |
| 新闻配图 | `src\assets\imgs\News\` |
| 首页视频 | `src\assets\mp4\` |

替换规则：**保持文件名不变**，用新图覆盖旧文件即可（尺寸比例最好和原图一致）。
要新增图片：放进对应文件夹，然后在 config.js 或页面代码里用 `require('./assets/imgs/xxx.png')` 引用。

---

## 第 4 步：生成最终网站文件（发布/部署时才需要）

```
npm run build
```

构建结果在 `public` 文件夹里，把整个 public 文件夹的内容上传到服务器即可。
（原来的 Dockerfile 里的 `gatsby serve` 方式仍然可用。）

---

## 常见问题

**Q: `npm install` 报一堆红色错误怎么办？**
A: 删除 `node_modules` 文件夹和 `package-lock.json`，重新执行 `npm install`。仍失败可把报错发给 Claude 帮忙看。

**Q: 打开 localhost:8000 是空白页或报错？**
A: 回到命令提示符看有没有红色报错；有的话先按 `Ctrl+C` 停掉，再重新 `npm run develop`。

**Q: 改了文字但浏览器没变化？**
A: zh.json / en.json 的文字改动需要重启开发服务器（Ctrl+C 停掉，重新 `npm run develop`，浏览器 Ctrl+F5）才能看到。确认改的是 `src` 目录下的文件（不要改 `public` 目录，那是生成的产物）。

---

## 技术备忘（供后续维护参考）

- 2026-08 已把 Gatsby 2.24/React 16 升级到 Gatsby 4.25/React 17/Webpack 5，支持 Node 16+。
- 迁移时修复的旧代码问题：`render({...rest})` 带参数、CSS 里的 `//` 注释、`aspect-ratio: 16/9` 斜杠、`@decorator` 语法（已保留支持）。
- `package-lock.json` 已随仓库保存，建议用 `npm install` 按锁安装。
