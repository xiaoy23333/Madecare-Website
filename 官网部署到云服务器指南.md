# 官网部署到云服务器指南

> 适用范围:冀德远健官网(测试域名 xiaoytestweb.online,阿里云 Linux 服务器)
> 最后更新:2026-08-18(按实际部署情况修订)

## 当前部署状态(已实测)

| 项目 | 实际情况 |
|------|---------|
| 服务器 | 阿里云 ECS,CentOS 7.4 64 位,公网 IP `47.104.163.193` |
| Web 环境 | Nginx 1.20.1(已装),**无可用宝塔面板**(8888 打不开) |
| SSH 登录 | 端口 **11162**(不是默认 22):`ssh -p 11162 root@47.104.163.193` |
| 网站目录 | `/var/www/madecare` |
| 预览地址 | `http://47.104.163.193:8081/`(8080 被管理软件占用,临时预览用 8081) |
| 备案 | ICP 备案进行中,未完成前域名访问 80 端口会被拦截,用 8081 端口预览 |
| 旧官网 | www.madecare.com 仍在本机 80 端口正常运行,勿动其配置 |

---

## 一、更新网站的完整流程(已跑通,照做即可)

### 第 1 步:电脑上构建(一条命令)

在装好项目的电脑上(cmd 窗口,项目目录 `D:\CC\1_madecare\project_SourceCode\official`):

```
npm run build:server
```

这一条命令会自动完成:复制手机版文件 + 设置无路径前缀 + 构建。等它跑完(几分钟)。

### 第 2 步:打包

把 `public` 文件夹里的**全部内容**打成 zip,命名为 `website.zip`(打包 public 里面的文件本身,不是把 public 文件夹打进去)。也可以由网站维护人协助生成此包。

### 第 3 步:上传到服务器

```
scp -P 11162 website.zip root@47.104.163.193:/tmp/
```

(密码注意:切英文输入法手动输入;连续输错会被临时封几分钟)

### 第 4 步:服务器上覆盖解压(SSH 登录后执行)

ssh登录
ssh -p 11162 root@47.104.163.193

```
rm -rf /var/www/madecare && mkdir -p /var/www/madecare && unzip -o /tmp/website.zip -d /var/www/madecare && chmod -R 755 /var/www/madecare && ls -la /var/www/madecare/index.html
```

**chmod 那条必须要有**——Windows 打包的文件解压后只有 root 能读,不执行会报 403。

### 第 5 步:验证

浏览器 `Ctrl+F5` 打开 `http://47.104.163.193:8081/`,按第"五"部分清单逐项检查。

---

## 二、Nginx 配置(已建好,备案通过后只需改一处)

当前配置在服务器 `/etc/nginx/conf.d/madecare.conf`,内容如下(备案期间用 8081 端口):

```nginx
server {
    listen 8081;
    server_name 47.104.163.193;

    root /var/www/madecare;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location = /news-data.json {
        add_header Cache-Control "no-cache";
    }
}
```

**备案通过后正式上线**,把 `listen 8081;` 改为 `listen 80;`、`server_name` 换成 `xiaoytestweb.online www.xiaoytestweb.online`,然后:

```bash
nginx -t
nginx -s reload
```

> 改配置用 `vi /etc/nginx/conf.d/madecare.conf`(按 `i` 编辑、`Esc`、`:wq` 保存)。旧官网的配置保持原样,两者互不影响。

---

## 三、域名解析(备案通过后再做)

1. 阿里云控制台 → 域名 → `xiaoytestweb.online` → 解析
2. 添加两条 A 记录,记录值都填 `47.104.163.193`:

| 记录类型 | 主机记录 | 记录值 |
|---------|---------|--------|
| A | @ | 47.104.163.193 |
| A | www | 47.104.163.193 |

3. 备案通过前**不要**配解析(未备案域名解析到国内服务器可能被检测拦截)。

---

## 四、发布动态(上线后的新方式)

直接改**服务器上**的 `/var/www/madecare/news-data.json`(SSH 用 `vi` 编辑,或用 WinSCP 下载改完再传),保存后刷新网站立即生效。数据格式与《发布动态操作指南.md》一致;改错了网站会自动退回内置内容,改回即可恢复。

---

## 五、部署后验证清单

- [ ] 电脑打开预览地址能显示首页(视频首屏)
- [ ] 滚轮翻屏正常,右上角中英文切换正常
- [ ] 首页底部"远健动态"轮播正常
- [ ] `/news/` 列表页正常,点条目能进详情
- [ ] 手机打开同一地址自动进手机版,各屏正常
- [ ] 手机版"远健动态"列表与详情正常
- [ ] 浏览器打开 `/news-data.json` 能看到 JSON 内容

---

## 六、常见问题

**Q: 打不开,显示 403?**
服务器上执行 `chmod -R 755 /var/www/madecare`(第 4 步里的权限命令漏了)。

**Q: 打开是 404?**
解压后文件是否多套了一层文件夹;Nginx 的 root 是否指向文件实际目录。

**Q: 8081 预览地址打不开?**
检查阿里云安全组是否放行了 8081 端口(入方向,TCP,来源 0.0.0.0/0)。

**Q: 想上 HTTPS?**
备案通过、域名解析生效后,可申请免费证书(无宝塔时用 certbot 或阿里云 SSL 证书 + Nginx 配置)。

**Q: 以后网站代码更新了怎么办?**
重复"一"的 1~5 步即可。
