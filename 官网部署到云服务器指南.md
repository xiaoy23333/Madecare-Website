# 官网部署到云服务器指南

> 适用范围:冀德远健官网(测试域名 xiaoytestweb.online,阿里云 Linux 服务器)
> 本指南分两部分:**A. 在电脑上构建网站文件**(由熟悉电脑操作的同事执行,约 10 分钟)和 **B. 在服务器上部署**(由登录服务器的同事执行)。

## 零、开始前必须知道的两件事

1. **ICP 备案(重要)**:域名放在阿里云国内服务器上,必须先完成 ICP 备案,否则阿里云会拦截访问,网站打不开。备案需要公司营业执照等材料,通常 1~4 周。**备案流程要现在就开始走**,与部署并行。
2. **网站是纯静态的**:构建出来的是一堆 HTML/JS/图片文件,服务器不需要装数据库、不需要 Node.js,只要能放静态文件(任何 Web 服务器)即可。

---

## A. 在电脑上构建网站文件

在**装好项目的那台 Windows 电脑**上操作(项目目录 `D:\CC\1_madecare\project_SourceCode\official`)。

### 第 1 步:构建"无前缀"版本(关键!)

服务器上的网站是根目录访问(不带 `/Madecare-Website/` 前缀),构建时必须设置一个空环境变量。

打开命令提示符(cmd),在项目文件夹地址栏输入 `cmd` 回车,然后:

```
cd /d D:\CC\1_madecare\project_SourceCode\official
set SITE_PREFIX=
npm run build
```

(如果用的 PowerShell,第二条命令换成:`$env:SITE_PREFIX=""; npm run build`)

等几分钟,出现 "Done building" 之类字样即为成功。

### 第 2 步:检查构建产物

构建完成后,打开 `public` 文件夹,确认里面**直接能看到**:

- `index.html`(而不是一个 Madecare-Website 子文件夹)
- `mobile` 文件夹(手机版)
- `news-data.json`(动态数据文件)

如果 `public` 里有一个 `Madecare-Website` 子文件夹,说明第 1 步的 `set SITE_PREFIX=` 没生效,需要重来。

### 第 3 步:打包

把 `public` 文件夹**里面的全部内容**打成 zip(注意:打包 public 里的文件本身,不是把 public 这个文件夹打进去)。命名为 `website.zip`。

> 注意:这是"服务器版"构建。以后还要更新 GitHub Pages 版时,正常跑 `npm run deploy` 即可,互不影响。

---

## B. 在服务器上部署

由登录阿里云服务器的同事操作。下面的步骤按服务器情况分两种写法,任选其一。

### 方案一:服务器上有宝塔面板(推荐,图形界面)

1. 登录宝塔面板,左侧点 **网站 → 添加站点**。
2. 域名填 `xiaoytestweb.online`,其他保持默认,提交。记下生成的**网站根目录路径**(通常在 `/www/wwwroot/` 下)。
3. 左侧点 **文件**,进入刚才的网站根目录,删除里面的默认文件(index.html、404.html)。
4. 把 `website.zip` 上传到该目录,右键解压,确认解压出来的 `index.html`、`mobile`、`news-data.json` **直接位于网站根目录下**(不是多套了一层文件夹)。
5. 完成,跳到"第 C 部分:域名解析"。

### 方案二:命令行(无宝塔,直接 Nginx)

> 适用:公司官网服务器实测 = 阿里云 ECS,C**entOS 7.4 64 位**,IP `47.104.163.193`,已装 **Nginx 1.20.1**,8888 端口无可用宝塔面板。

#### 0. 先做服务器自检(登录后依次执行)

```bash
# ① 确认 nginx 已装及版本(应输出 nginx version: nginx/1.20.1)
nginx -v

# ② 看看有没有宝塔残留(有输出=装过宝塔,但面板坏了,不用管它,直接走 nginx 方案)
ls /www/server/panel 2>/dev/null && echo "有宝塔残留" || echo "无宝塔"

# ③ 确认解压工具(提示 command not found 就装一下)
which unzip || yum install -y unzip

# ④ 看防火墙(输出 running 表示开着,需要放行 80 端口)
systemctl status firewalld | head -3
```

若第 ④ 步显示防火墙在运行,执行放行(80 端口旧官网已通,一般已放行,这步保险):

```bash
firewall-cmd --permanent --add-service=http --add-service=https
firewall-cmd --reload
```

#### 1. 上传网站文件

用 WinSCP(免费图形工具)或 scp 把 `website.zip` 传到服务器:

```
scp website.zip root@47.104.163.193:/tmp/
```

#### 2. 解压到新目录(不动旧官网任何文件)

```bash
mkdir -p /var/www/madecare
cd /var/www/madecare
unzip /tmp/website.zip
ls /var/www/madecare   # 应看到 index.html、mobile、news-data.json
```

**重要:解压后必须执行下面这条权限命令**(Windows 打包的文件解压后只有 root 能读,Nginx 读不了会报 403):

```bash
chmod -R 755 /var/www/madecare
```

> 以后每次重新上传更新网站文件,解压后都要再跑一次这条 chmod 命令。

#### 3. 新增 Nginx 站点配置(CentOS 7 下 Nginx 配置目录)

**不要动旧官网的配置**,新建一个配置文件即可。先看一眼现有配置文件名,避免重名:

```bash
ls /etc/nginx/conf.d/
```

然后创建新配置(文件名避开上面已存在的,例如叫 `madecare.conf`):

```bash
vi /etc/nginx/conf.d/madecare.conf
```

(vi 使用方法:按 `i` 进入编辑 → 粘贴下面内容 → 按 `Esc` → 输入 `:wq` 回车保存)

```nginx
server {
    listen 80;
    server_name xiaoytestweb.online www.xiaoytestweb.online;

    root /var/www/madecare;
    index index.html;

    # 静态文件直接返回;找不到的路径回退首页(兼容前端路由)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 动态数据文件不缓存,同事改完立即生效
    location = /news-data.json {
        add_header Cache-Control "no-cache";
    }
}
```

#### 4. 检查并重载 Nginx

```bash
nginx -t
nginx -s reload
```

> 已有其他网站占用了 80 端口?给 Nginx 配虚拟主机即可,一个端口可以跑多个域名,不影响。旧官网的配置保持原样,新网站通过域名 xiaoytestweb.online 区分。

---

## C. 域名解析

1. 阿里云控制台 → **域名** → 找到 `xiaoytestweb.online` → **解析**。
2. 添加两条 A 记录:

| 记录类型 | 主机记录 | 记录值 |
|---------|---------|--------|
| A | @ | 47.104.163.193 |
| A | www | 47.104.163.193 |

3. 等待生效(几分钟到几小时不等)。**前提是备案已完成**,否则国内服务器会拦截访问。

---

## D. 部署后验证清单

在浏览器里逐项检查:

- [ ] 电脑打开 `http://xiaoytestweb.online/` 能显示首页(视频首屏)
- [ ] 鼠标滚动/滚轮能翻屏,右上角能切中英文(切换后能正常显示)
- [ ] 首页底部"远健动态"能显示新闻轮播
- [ ] 打开 `/news/` 新闻列表页,点一条能进详情
- [ ] 手机打开同一网址,自动进入手机版首页
- [ ] 手机版进入"远健动态",列表能显示,点条目能看详情
- [ ] 浏览器打开 `http://xiaoytestweb.online/news-data.json`,能看到 JSON 内容

全部通过 = 部署成功。

---

## E. 部署上线后的两个变化(同事须知)

1. **发布动态的新方式**:直接改**服务器上**的 `news-data.json`(宝塔"文件"里在线编辑,或下载改完再上传),保存后刷新网站立即生效。不再需要碰 GitHub。数据格式和之前一致,格式改错了网站会自动退回内置内容,改回即可恢复。
2. **图片**:动态配图可以上传到服务器任意目录(如 `/images/`),数据里写相对网址 `images/xxx.jpg` 即可。

---

## F. 备案前的临时预览方案(可选)

ICP 备案通常要 2~3 周。在备案下来之前,如果想让同事/手机从外网提前看新网站效果,可以用**非标准端口**临时挂载(备案拦截只管 80/443 端口的域名访问,IP+其他端口不受影响):

1. 上面方案二的 Nginx 配置里,把 `listen 80;` 临时改成 `listen 8080;`(先不要配域名解析)
2. 阿里云控制台 → 云服务器 ECS → 安全组 → 放行 8080 端口(放行方式与 80 相同,端口填 8080)
3. `nginx -t && nginx -s reload`
4. 访问 `http://47.104.163.193:8080/` 即可看效果(手机也能打开,设备自动跳转和动态数据都正常)
5. 备案通过后,把配置改回 `listen 80;`,加上域名解析,正式上线

> 期间不要给域名配置解析到该服务器(未备案域名解析过去可能被阿里云检测拦截并通知)。等备案号下来再解析。

---

## G. 常见问题

**Q: 域名解析了、网站也放好了,但打不开?**
十有八九是 ICP 备案还没完成(国内服务器拦截)。在阿里云控制台查备案状态;备案完成前可用服务器 IP 加端口临时验证(注意国内 80 端口也可能被拦)。

**Q: 打开是 404?**
检查第 B 部分第 4 步:解压后文件是否多套了一层文件夹;Nginx 的 root 是否指向了文件实际所在目录。

**Q: 想上 HTTPS?**
宝塔面板里给站点点 SSL,一键申请免费证书即可(需要域名解析已生效)。

**Q: 以后网站代码又更新了怎么办?**
在电脑上重新执行 A 部分第 1~3 步,把新的 website.zip 覆盖上传到服务器网站目录即可。
