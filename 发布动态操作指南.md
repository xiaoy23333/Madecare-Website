# 发布动态操作指南(冀德远健官网)

本指南面向需要发布/删除"远健动态"内容的同事。整个过程**不需要懂代码**,照步骤点即可。

## 原理一句话

网站的动态内容全部来自 GitHub 上的一个数据文件 `static/news-data.json`。
改这个文件 = 改网站内容;改完保存后,网站(PC 版 + 手机版中英文)自动读取最新内容。

---

## 一、打开编辑页面

1. 浏览器打开(建议用 Chrome / Edge 浏览器):
   `https://github.com/xiaoy23333/Madecare-Website/blob/master/static/news-data.json`
2. 点右上角的 **铅笔图标**(编辑按钮)。若提示登录,输入公司共用的 GitHub 账号密码。
3. 页面会变成可编辑的文本框,里面就是全部动态内容。

> 若 GitHub 网页打不开或很慢(国内网络偶尔如此),可挂代理后再试,或把要发的内容发给维护网站的同事代发。

## 二、新增一条动态

### 模板(复制这一段,粘到文件里合适的位置)

```json
{
  "id": "news20240301",
  "title": "标题:例如:冀德远健参加第XX届医疗器械博览会",
  "time": "2024-03-01",
  "publisher": "冀德远健",
  "cnt": "这里写一句话简介,会显示在列表里,建议60字以内。",
  "src": "",
  "body": [
    { "t": "txt", "v": "正文第一段。这里是动态的正文内容。" },
    { "t": "title", "v": "小标题(可选,不需要就删掉这一行)" },
    { "t": "img", "v": "https://图片完整网址" },
    { "t": "txt", "v": "正文第二段。" }
  ]
}
```

### 各字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| id | 是 | 每条唯一的英文代号,不能重复,不能有空格。建议用"news+日期",如 news20240301 |
| title | 是 | 动态标题 |
| time | 是 | 发布日期,格式年-月-日 |
| publisher | 否 | 发布者,默认写 冀德远健 |
| cnt | 是 | 一句话简介,显示在列表页 |
| src | 否 | 封面图网址。**没有图就写 ""(两个英文引号)** |
| body | 是 | 正文,由一段段组成,每段是 `{ "t": "类型", "v": "内容" }` |

### body 的四种段落类型

- `{ "t": "txt", "v": "文字内容" }` — 一个正文段落
- `{ "t": "title", "v": "小标题文字" }` — 加粗小标题(可选)
- `{ "t": "img", "v": "图片网址" }` — 一张图(可选)
- `{ "t": "video", "v": "视频网址" }` — 一个视频(可选)

段与段之间用英文逗号 `,` 分隔,**最后一段后面不要加逗号**。

### 具体操作

1. 找到文件里 `"zh": [` 这一行,它后面是所有中文动态。
2. 在**第一条动态的 `{` 之前**插入上面的模板(新动态会显示在列表最上面)。
   或者在最后一条之后插入(显示在最下面)。
3. 把模板里的内容改成真实的标题、日期、简介、正文。
4. **注意逗号**:新条目和原有条目之间要用英文逗号 `,` 隔开。

### 图片怎么填

- 如果图片已经上传到公司服务器:填完整网址,如 `"https://你的域名/images/xxx.jpg"`。
- 也可以用任何公开图床/微信文章里的图片网址。
- 不带图:**`"src"` 写 `""`,正文里也不放 img 段落**,完全没问题。

## 三、删除一条动态

找到该动态的 `{ "id": "xxx", ... }` 整段(从 `{` 到对应的 `}`),连同它前后的一个逗号一起删掉,保证剩下的条目之间仍然用逗号正确分隔。

## 四、保存(发布)

1. 改完后,页面往下拉,点绿色的 **Commit changes**(提交更改)按钮。
2. 完成!网站会自动读取最新内容。
3. **生效时间**:通常几分钟内生效;最长可能要等 12 小时(CDN 缓存)。
   如需立刻生效,用浏览器打开下面这个地址点一下(会自动刷新缓存,无需登录):
   `https://purge.jsdelivr.net/gh/xiaoy23333/Madecare-Website@master/static/news-data.json`
   打开后显示 purge 字样即可,再刷新网站页面就能看到新内容。

## 五、验证与排错

**发布后到网站确认**:电脑版 `https://xiaoy23333.github.io/Madecare-Website/news/`,手机版从首页进入"远健动态"。

**如果全站动态都不显示了**:说明刚才改的文件格式有错(最常见是逗号漏加或引号没成对)。处理办法:

1. 重新打开编辑页面;
2. 点右上角 **History**(历史记录);
3. 找到上一次正常版本,点它右边的 `<>` 图标(查看该版本);
4. 再点右上角铅笔图标编辑这个旧版本,直接保存 —— 即可恢复。

**在线检查格式**:把整个文件内容复制到 `https://www.json.cn` 的在线校验工具,点校验,报错会指出第几行有问题。

---

## 附:网站将来上云服务器后

数据文件会放到公司服务器上,编辑方式会更简单(直接改服务器上的同名文件,即时生效)。届时本指南会更新,网站代码无需改动。
