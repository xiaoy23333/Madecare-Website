# 发布动态操作指南(冀德远健官网)

> 最后更新:2026-08-18
> 适用范围:网站已部署到公司服务器后,增删改"远健动态"内容
> 全程**不需要懂代码**,照步骤点即可

## 一句话原理

网站的"远健动态"内容全部来自服务器上的一个数据文件:

```
/var/www/madecare/news-data.json
```

**改这个文件 = 改网站内容**。改完保存,刷新网站(电脑版 + 手机版中英文)立即生效,不用重新构建、不用重启。

---

## 方式一:WinSCP 图形界面(推荐,最直观)

### 第 1 步:下载安装 WinSCP

浏览器打开 `https://winscp.net/eng/download.php`(或搜索"WinSCP 下载"),下载并安装。

### 第 2 步:连接服务器

打开 WinSCP,登录信息按下面填:

| 项目 | 填写内容 |
|------|---------|
| 文件协议 | SFTP(默认) |
| 主机名 | 47.104.163.193 |
| 端口号 | 11162 |
| 用户名 | root |
| 密码 | 服务器密码 |

填完点"登录"。

### 第 3 步:找到数据文件

连接成功后,右侧是服务器文件。按路径依次进入:

```
/var/www/madecare/news-data.json
```

### 第 4 步:编辑并保存

- **双击** `news-data.json` 打开编辑器(首次会问用什么软件,选"内部编辑器"或记事本)
- 按下面的"数据格式"修改内容
- **Ctrl+S 保存**,WinSCP 会询问"是否上传修改后的文件",点**"是"**

完成!网站立即生效(如果浏览器没变化,按 Ctrl+F5 强制刷新)。

---

## 方式二:SSH 命令行(适合熟悉服务器的人)

```
ssh -p 11162 root@47.104.163.193
vi /var/www/madecare/news-data.json
```

vi 操作:按 `i` 编辑 → 改完按 `Esc` → 输入 `:wq` 回车保存。

---

## 数据格式

文件是 JSON 格式,结构如下(中文动态在 `zh` 里,英文在 `en` 里):

```json
{
  "version": 1,
  "zh": [
    {
      "id": "news20240301",
      "title": "动态标题",
      "time": "2024-03-01",
      "publisher": "冀德远健",
      "cnt": "一句话简介,显示在列表页,建议60字以内",
      "src": "",
      "body": [
        { "t": "txt", "v": "正文第一段。" },
        { "t": "title", "v": "小标题(可选)" },
        { "t": "img", "v": "图片地址(可选)" },
        { "t": "txt", "v": "正文第二段。" }
      ]
    }
  ],
  "en": [ ...同样格式... ]
}
```

### 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| id | 是 | 每条唯一代号,不能重复、不能有空格。建议"news+日期",如 news20240301 |
| title | 是 | 动态标题 |
| time | 是 | 发布日期,格式 年-月-日 |
| publisher | 否 | 发布者,默认 冀德远健 |
| cnt | 是 | 一句话简介,显示在列表页 |
| src | 否 | 封面图地址。**没有图就写 ""(两个英文引号)** |
| body | 是 | 正文,由一段段组成,段与段用英文逗号分隔,**最后一段后不加逗号** |

### body 的四种段落类型

- `{ "t": "txt", "v": "文字内容" }` — 正文段落
- `{ "t": "title", "v": "小标题文字" }` — 加粗小标题(可选)
- `{ "t": "img", "v": "图片地址" }` — 一张图(可选)
- `{ "t": "video", "v": "视频地址" }` — 一个视频(可选)

### 新增一条动态

1. 在 `zh` 的 `[` 后面(第一条之前)插入上面的模板——新动态会显示在最上面
2. 改成真实内容,注意**每条之间用英文逗号 `,` 隔开**
3. 英文站也要发的话,把同样的条目(英文版)加到 `en` 里;只发中文就只改 zh

### 删除一条动态

找到该条的 `{ "id": "xxx", ... }` 整段(从 `{` 到对应的 `}`),连同它前后的一个逗号一起删掉。

---

## 图片怎么填

- **已有外链图**:直接填完整网址,如 `https://图片地址.jpg`
- **上传到服务器**(推荐,稳定):用 WinSCP 把图片传到 `/var/www/madecare/news-imgs/` 目录,数据里填相对路径:
  ```
  "src": "news-imgs/我的图片.jpg"
  ```
- **不带图**:`src` 写 `""`,正文里不写 img 段落,完全没问题

---

## 发布后验证

1. 电脑打开网站 → 首页底部"远健动态"轮播 → 看新动态是否出现
2. 打开"远健动态"列表页 → 点新动态 → 看详情内容
3. 手机打开网站 → "远健动态" → 同样检查
4. 没变化就按 **Ctrl+F5** 强刷(手机清一下浏览器缓存)

---

## 重要提醒(必读)

1. **改之前先备份**:双击打开文件后,先把全部内容复制存到一个文本文档里。改错了可以直接粘贴回来恢复。

2. **网站代码更新时小心覆盖**:公司更新网站代码(重新上传 website.zip 解压)时,会用包里的旧数据文件**覆盖掉你改过的 news-data.json**,导致动态内容丢失。
   遇到网站更新,更新后记得把备份的动态数据重新改回服务器上的 news-data.json,或者提前把最新数据发给维护网站的人,让他打进包里。

3. **格式错了怎么办**:如果改完网站动态区全空了,说明 JSON 格式有错(最常见:逗号漏加、引号没成对)。把备份的内容重新贴回去保存即可恢复。
   也可以把文件内容复制到 `https://www.json.cn` 在线校验,报错会指出第几行有问题。

---

## 常见问题

**Q: 保存后网站没变化?**
Nginx 对 news-data.json 已配置不缓存,正常是立即生效。先按 Ctrl+F5 强刷;手机浏览器缓存顽固,换无痕窗口试。

**Q: 手机版显示了,电脑版没有?**
电脑版也是读同一个文件,强刷一下就会同步。若仍不同,检查是不是看成了旧标签页。

**Q: 动态图片不显示?**
检查图片地址:相对路径(如 `news-imgs/xxx.jpg`)要求文件确实传到了服务器该目录;外链图可能被对方网站防盗链拦截,优先用上传到服务器的方式。

**Q: 想改现有某条动态?**
找到它的 `id`,直接改 title/cnt/body 里对应的文字,保存即可。
